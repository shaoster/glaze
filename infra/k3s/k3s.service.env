# k3s Go runtime tuning.
# GOMEMLIMIT must be above k3s's actual working set (~450MB RSS) or the runtime
# enters continuous GC mode, which starves kine polling goroutines and causes
# them to accumulate to ~9k, pinning the CPU at 80%+.
GOGC=50
GOMEMLIMIT=600MiB
