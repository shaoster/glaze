apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: glaze-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: infisical
    kind: ClusterSecretStore
  target:
    name: glaze-secrets
    creationPolicy: Owner
  dataFrom:
    - find:
        path: /
